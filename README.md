To run the project go to the /front-end dir where package.json is and start the React application by npm start - it will start on http://localhost:3000/. 
Start the Spring application so the front-end can connect to the back-end, the REST API runs on http://localhost:8080/
